# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# `dart build cli` writes the compiled executable here. This package declares
# one, so without this line a 5 MB binary lands in the published archive.
build/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock
